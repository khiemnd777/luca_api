# scripts/create_module/templates/config.yaml.tmpl
server:
  host: ${M_AUTH_HOST}
  port: ${M_AUTH_PORT}
  route: "/api/auth"
external: true
database:
  provider: ${DB_PROVIDER}  # or "mongodb"
  automigrate: true
  postgres:
    host: ${PG_HOST}
    port: ${PG_PORT}
    user: ${PG_USER}
    password: ${PG_PASSWORD}
    name: ${PG_DBNAME}
    sslmode: ${PG_SSL}
