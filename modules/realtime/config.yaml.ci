# scripts/create_module/templates/config.yaml.tmpl
server:
  host: ${M_REALTIME_HOST}
  port: ${M_REALTIME_PORT}
  route: "/ws"

external: true

database:
  provider: ${DB_PROVIDER} # or "mongodb"
  automigrate: true
  postgres:
    host: ${PG_HOST}
    port: ${PG_PORT}
    user: ${PG_USER}
    password: ${PG_PASSWORD}
    name: ${PG_DBNAME}
    sslmode: ${PG_SSL}